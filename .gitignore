# ───── Python ─────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# ───── 虚拟环境 ─────
.venv/
venv/
env/
ENV/

# ───── 运行时数据（每个会话的工作文档，由程序自动创建）─────
sessions/

# ───── 元数据库（SQLite，含 users/sessions/library；勿提交）─────
data/
*.db
*.db-wal
*.db-shm

# ───── 密钥与本地配置 ─────
.env
.env.local
*.local

# ───── 测试 / 类型检查缓存 ─────
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ───── 编辑器 / 操作系统 ─────
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db

# ───── 临时上传 / 导出 ─────
*.tmp
~$*.docx

# ───── 前端构建（frontend/ 为 Vite+React 源码；构建产物提交到 static/）─────
frontend/node_modules/
frontend/.vite/
.vite-check/
